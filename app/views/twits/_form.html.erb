<%= form_for @twit = Twit.new, html: { multipart: true } do |f| %>
  <% if @twit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@twit.errors.count, "error") %> prohibited this twit from being saved:</h2>

      <ul>
      <% @twit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" >
    <%= f.label :image %><br />
    <%= f.file_field :image, autofocus: true %>
  </div>
  
  <div class="field" >
    <%= f.text_area :body, id:"text-area", maxlength: 140 %>
  </div>
  
  <div class="actions" id="submit">
    <%= f.submit %>
  </div>
  
<% end %>
